# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# Segredos — NUNCA versionar
.env
*.secret
secrets/
*.key
config/secrets.json

# Dados derivados / histórico (regeneráveis; backup vive em 2 cópias, não no git)
historico/
dados.local.json
# OBS: app/dados.json É versionado — é a cópia que o Cloudflare (pasta app/) serve.
# Mantê-la em sincronia com o canônico da raiz (o gerador escreve os dois).

# refs de design (stock/terceiros — não versionar)
Fotos Exemplo/

# Sinalizador do ralph loop
LOOP_DONE

# Claude Code — config local (especifica da maquina/sessao; nao compartilhar)
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# Segredos — NUNCA versionar
.env
*.secret
secrets/
*.key
config/secrets.json

# Dados derivados / histórico (regeneráveis; backup vive em 2 cópias, não no git)
historico/
dados.local.json
# cópia local p/ servir o app em dev (o canônico fica na raiz)
app/dados.json

# refs de design (stock/terceiros — não versionar)
Fotos Exemplo/

# Sinalizador do ralph loop
LOOP_DONE

# Claude Code — config local (especifica da maquina/sessao; nao compartilhar)
.claude/settings.local.json
